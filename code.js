function evenOrOdd(n) { //for string lenght take string}
    if (n % 2 === 0) {//and replace numm with string.lenght here}
        return "even";
    } else {
        return "odd";
    }
}

function greeting(name) {
    return "Hola, "+(name) + "!";
    //Returns "Hola, Nika!"
    greeting("Nika");
}